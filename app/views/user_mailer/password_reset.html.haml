%h1 Password Reset
%p= t(:click_to_reset)

= link_to t(:reset_password), edit_password_reset_url(reset_token: @user.reset_token,
                                                      email: @user.email)

%p= t(:reset_link_expire)